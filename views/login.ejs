<!DOCTYPE html>
<html lang="ja">
  <head>
    <meta charset="utf-8">
     <meta name="viewport"
        content="width=device-width, initial-scale=1.0, maximum-scale=1.0, user-scalable=no" />
    <title>Sentence Folder - Login</title>
    <link href="/stylesheets/reset.css" rel="stylesheet">
    <link href="/stylesheets/app.css" rel="stylesheet">
  </head>
  <body id="login">
    <%- partial('_header', {menu: false}) %>
    <form id="login-form" method="post" action="/login">
      <%if(input_error){%>
      <p class="error large-x">ログインIDもしくはパスワードが正しくありません。</p>
      <%}%>
      <input type="text" name="login_id" class="<%=input_error ? 'error' : ''%>"
            placeholder="Account ID" value="<%=login_id || ''%>">
      <input type="password" name="password"
            class="<%=input_error ? 'error' : ''%>"
            placeholder="Password" value="<%=password || ''%>">
      <label class="large-x" for="auto-login">オートログインを有効にする</label>
      <input type="checkbox" id="auto-login" name="auto_login" value="true" <%=auto_login ? 'checked':''%>>
      <input type="submit" class="btn-medium" value="ログイン"  >
    </form>
    <footer>
    </footer>
  </body>

</html>


